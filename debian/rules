#!/usr/bin/make -f
# -*- makefile -*-
# Sample debian/rules that uses debhelper.
# This file was originally written by Joey Hess and Craig Small.
# As a special exception, when this file is copied by dh-make into a
# dh-make output file, you may use that output file without restriction.
# This special exception was added by Craig Small in version 0.37 of dh-make.

# Uncomment this to turn on verbose mode.
export DH_VERBOSE=1
export DESTDIR=$(CURDIR)/debian/u-boot-marvell-clearfog

%:
	dh $@ --parallel --sourcedirectory=u-boot --builddirectory=u-boot

override_dh_auto_configure:
	sed -i "s;-msoft-float;-mfloat-abi=hard -mfpu=vfpv3;g" u-boot/arch/arm/cpu/armv7/config.mk
	cd u-boot; make armada_38x_clearfog_config
	cd u-boot; make u-boot.mmc

override_dh_auto_install:
	cd u-boot; install -v -D -m 0644 u-boot.mmc $(DESTDIR)/boot/u-boot-clearfog.mmc
